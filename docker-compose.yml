services:
  app:
    build: .
    container_name: user-service-test
    depends_on:
      - mongo
    environment:
      MONGO_URI: mongodb://mongo:27017/test-db
    command: npm test

  mongo:
    image: mongo:6
    container_name: mongo-test
    ports:
      - "27017:27017"